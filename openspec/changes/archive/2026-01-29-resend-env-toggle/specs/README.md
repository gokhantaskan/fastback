# Specs

No spec changes for this change.

This is a configuration enhancement (adding `ENABLE_RESEND` toggle) with no new capabilities or requirement modifications. The existing email-sending behavior is preserved; the toggle only controls whether `resend.Emails.send()` is invoked.
